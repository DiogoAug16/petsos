import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    top: -95,
    alignSelf: 'center',
    zIndex: 10,
    right: -90,
  },
  image: {
    width: 300,
    height: 300,
  },
});

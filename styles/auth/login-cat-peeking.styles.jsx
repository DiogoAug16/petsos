import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    top: -180,
    alignSelf: 'center',
    zIndex: 10,
    right: -80,
  },
  image: {
    width: 300,
    height: 300,
  },
});

import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  pawPrint: {
    position: 'absolute',
    width: 40,
    height: 40,
    zIndex: 0,
  },
  pawImage: {
    width: '100%',
    height: '100%',
  },
});

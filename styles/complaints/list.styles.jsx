import { StyleSheet } from 'react-native';

export const listStyles = (colorScheme) => {
  return StyleSheet.create({
    listContent: {
      padding: 14,
      gap: 10,
      paddingBottom: 80,
    },
  });
};
